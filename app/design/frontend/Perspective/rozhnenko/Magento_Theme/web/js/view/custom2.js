define(['uiComponent'], uiComponent => {
    'use strict';

    return uiComponent.extend({
        defaults: {
            template: 'Magento_Theme/customMultiply'
        },

        initialize() {
            this._super();
        }
    });
})
